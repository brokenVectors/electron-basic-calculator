{
  "name": "electron-test",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --watch . --exec \"./node_modules/.bin/electron . --enable-logging --app-dir=/Users/miles/webdev\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^16.0.7"
  },
  "dependencies": {
    "nodemon": "^2.0.15"
  }
}
